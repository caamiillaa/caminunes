import React, { Component } from "react";

class Header extends Component {
	render() {
		return (
			<header className="header">
				<p className="header--title">Hi, I'm Cami Nunes (:</p>
			</header>
		);
	}
}

export default Header;
